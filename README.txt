The schema repo is a standalone version of the patch submitted on the Apache AVRO-1124 ticket.

In order to build and run the schema repo, execute the following commands in the current directory:

mvn install
./run.sh
